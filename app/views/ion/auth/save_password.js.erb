$('.ion-win').unblock();
ion.flash.rails( <%= raw flash.to_json %> );
<% if flash.notice %>
  ion.Win.closeAll();
<% else %>
  $('.ion-win-title').html($('.ion-flash-container').html());
  $('.ion-win input[type=password]:first').select();
<% end %>