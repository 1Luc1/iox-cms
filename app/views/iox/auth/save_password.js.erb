$('.iox-win').unblock();
iox.flash.rails( <%= raw flash.to_json %> );
<% if flash.notice %>
  iox.Win.closeAll();
<% else %>
  $('.iox-win-title').html($('.iox-flash-container').html());
  $('.iox-win input[type=password]:first').select();
<% end %>